# Port the worker listens on
PORT=8787

# Shared bearer token — MUST match the WORKER_SHARED_SECRET set in Convex env.
# Generate with: openssl rand -base64 32
WORKER_SHARED_SECRET=replace_me

# Set to 1 to watch the headless browser while debugging a scraper locally.
# SHOW_BROWSER=1
